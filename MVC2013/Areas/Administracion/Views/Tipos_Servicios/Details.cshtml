@model MVC2013.Models.Tipos_Servicios

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Administracion/Views/Shared/_Layout.cshtml";
}

<h2>Detalles del Servicio</h2>

<div>
    <hr />
    <div class="panel panel-info">
        <div class="panel-heading bg-light-blue">Informacion del Servicio Adicional</div>
        <div class="panel-body">
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.nombre)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.nombre)
                </dd>
            </dl>
        </div>
    </div>
</div>
<p>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button class="btn btn-sm btn-bitbucket" onclick="location.href = '@Url.Action("Edit", new { id = Model.id_tipo_servicio })'">
                <span class="glyphicon glyphicon-edit"></span> @Resources.Resources.editar
            </button>
        </div>
    </div>

    <div>
        <button class="btn btn-sm btn-primary" onclick="location.href = '@Url.Action("Index")'">
            <span class="glyphicon glyphicon-backward"></span>&nbsp;@Resources.Resources.listado_regresar
        </button>
    </div>

</p>
